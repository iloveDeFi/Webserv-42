```
/* swaps first 2 elements top of list A */
void	sa(t_nlist **head_a)
{
	t_nlist	*ele1;
	t_nlist	*ele2;

	ele1 = *head_a;
	ele2 = (*head_a)->next;
	if (!ele1 || !ele2)
		return ;
	ele1->next = ele2->next;
	ele2->next = ele1;
	*head_a = ele2;
	ft_printf("sa\n");
}
```

| 1 | 2 | 3 |
| --- | --- | --- |
| met a jour pour qu il pointe vers 2 | met a jour pour qu il pointe sur 3 | met a jour pour qu il pointe vers le 1 |
|  |  |  |
